import React from 'react';

import { Button } from 'fastUI';

export default () => {
  return (
    <>
      <Button type="default">默认按钮</Button>&nbsp;
      <Button type="primary">按钮</Button>
    </>
  );
};
